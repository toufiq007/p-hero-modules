/** higer order function
 * map
 * filter
 * forEach
 * find
 * reduce
 */

// .reduce(accumulatorFunction,initial value)

// accumulatorFunction has two parameters
// currentValue,total


const numbers = [1,2,3,4,5]
const total = numbers.reduce((current,previous)=> previous+=current,0)
console.log(total)

// reduce can be use in many ways
// we will see this later












